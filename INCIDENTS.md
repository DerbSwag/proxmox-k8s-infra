# Incident Log

<!-- ?????? incident ?????????????? ??????????????????????? -->

## Template

```
## [YYYY-MM-DD] — Brief Title

**Problem:**
??????????????????

**Root Cause:**
??????

**Fix:**
?????????

**Result:**
????????????????

**Lessons Learned:**
??????????????????
```

---

<!-- ????? incident ??????????????? -->

## [2026-06-17?19] — pve02 host instability (flapping) — OPEN

**Problem:**
pve02 (10.0.1.2) ???-??? ??????? 2+ ??? ? worker-02 NotReady, pve02+worker-02 monitoring ???, Proxmox cluster non-quorate (1/2)

**Root Cause (????? hardware):**
????? ping ?????? services ??? ? ??????? ? ?????? ????????? power cycle = RAM/disk/PSU/overheat (?????? software)

**Fix (????????):**
- power-cycle ?????????? ? ?????? (uptime ????)
- ?????????: `pvecm expected 1` ?? pve01 + cordon worker-02
- VM onboot=1 ? worker-02 autostart ????? pve02 ????

**Result:**
?????? pve02 ?????? (3 nodes Ready, quorate, ??? workload ????) ??? **???????????? — ???????? hardware ??????????** (LED/BIOS/memtest/SMART)

**Lessons Learned:**
- ping-but-services-dead ????? = hardware ?????? software
- summary/inhibit alerts ??????? — ???? SingleNodeDown/KubeNodeNotReady ??? ??? flood
- ?? VM 210 backup ?????? pve02 disk — ??? disk ???? backup ?????? ??? copy ???

## [2026-06-17] — ? RESOLVED: worker host-port block (??? case 13 ???) + AppXSvc suppress

**Problem:**
worker 9100/10250 block cross-node ????????? 4 ??.?. (kubectl top `<unknown>`, exec 502, TargetDown)

**Root Cause (2 ????????????):**
1. dual-IP — netplan `50-cloud-init.yaml` (dhcp4:true) ??? DHCP IP ??????? static ? asymmetric routing
2. UFW ????? rule 9100/10250 (?????? DR rebuild 16 ??.?.)

**Fix:**
???? 2 workers ???? SSH + auto-revert safety net (?????????? console ???????): ?? 50-cloud-init + disable cloud-init net + `ufw allow 9100/10250` + netplan apply ? ????? static IP ?????

**Result:**
? ???? 3 nodes: 9100+10250 OPEN, Prometheus targets UP ???, `kubectl top` ???, `kubectl exec` worker pod ??? (??? 502) — ??? case ??????? 4 ??.?.

**?????????:** suppress AppXSvc flapping (trigger-start service ?????? GoogleUpdater) ?? Windows template macro

**Lessons Learned:**
- ???? `ip addr`/netplan ?????????????????? host-port/routing issue (??????????? k3s ???)
- ???????????????????????? — ??? DHCP ?????????????? UFW
- scripts ??? Windows ???? strip CRLF (`tr -d '\r'`) ????????? Linux
- safety-net auto-revert ?????????? network ???? SSH ????????????? console (worker ????????? master)

## [2026-06-16/17] — DR recovery ???????????? + ????? CCTV-04 (?????????????)

**???????? DR (?????? incident full-cluster-loss):**
- Zabbix monitoring ????????? **15 hosts UP**: Linux×3, Proxmox×2 (link Linux template, 194 items/host), Windows×5, CCTV×4 (SNMP v2c community public, Generic by SNMP template)
- pve01/pve02 ????? Zabbix agent monitoring (???? DR doc ?????? HTTP template — ??? Linux agent template ?????????? ??? disk ? ??? backup-disk-full ???)
- **????? CCTV-04 (10.0.1.x)** 2026-06-16 ? UP
- Lark routing: 3 media types (Linux/Windows/CCTV) + 3 actions, Windows ??? group, GoogleUpdater macro re-applied
- Vault auto-unseal CronJob verified, k3s+PV backup off-node daily, Proxmox VM backup fixed (retention keep-last=1)

**Runbook ?????:** ???? add host ???? Zabbix (agent/SNMP) ???? API — `RUNBOOK.md` section "Add a host to Zabbix"

**???????????????:** netplan duplicate-IP fix (worker host-port metrics) — ?? maintenance window + Proxmox console

**Lessons Learned:**
- ???? host inventory ?? `scripts/zabbix-readd-hosts.sh` + README ??? sync ???????????????? host ? DR ????????? reprovision ???????????

## [2026-06-16] — ?? ROOT CAUSE: worker host-port block = netplan static IP ??? .140 ??? node

**Problem:**
worker 9100/10250 block cross-node ????????? 4 ??.?. (???????????????? ??? clean reinstall)

**Root Cause (??????????):**
??? node clone ??? master template ????????? IP ? netplan `00-installer-config.yaml` ???? static **10.0.1.10 ??????? 3 ???????** + `50-cloud-init.yaml` ???? dhcp4 ? ????? node ?? .140(???)+DHCP(.94/.166/.167) ? asymmetric routing ? host-port reply ??? drop (NodePort/pod-network ??????????? path ????, master scrape ?????????????? same-node)

**Fix:**
????????? — ???? maintenance window + Proxmox console (??????? IP ?????? SSH ????) ?? runbook ????????? `docs/incidents/2026-06-16-worker-duplicate-ip-rootcause.md` (netplan static unique IP, ?? DHCP, `netplan try`, ??????? node, master ???????)

**Lessons Learned:**
- ?????? worker-01 ?????????????? 4 ??.?. = network config ?????? k3s — ??????????? k3s/firewall ????????????????? netplan ??????????
- VM template ?? master static IP ????? ? clone ??????????? IP ????
- ???? mix static + DHCP ?? interface ?????

## [2026-06-16] — ?? CRITICAL: Full cluster loss ??????? worker-01 reinstall (DR rebuild)

**Problem:**
??????? reinstall worker-01 (??? host-port) ??? uninstall/killall ????????? cluster ??? — master k3s ?????? (binary/units/datastore) + local-path PV data (Zabbix DB + Vault) ?????????

**Root Cause:**
- worker-01 ????? ???? k3s SERVER install (?????? agent) — `k3s-uninstall.sh` ?? /var/lib/rancher/k3s ???? tree ??? PV storage
- master ?????? k3s state ??????????? (propagation path ??????)

**Fix (Option B — accept data loss, rebuild from git):**
??? DR Scenario 3: fresh k3s server ?? master ? rejoin workers ? redeploy stack (prometheus/loki/zabbix/vault/argocd) via Helm ? apply manifests ? re-bootstrap Vault ? Zabbix fresh

**Result:**
3 nodes Ready, ??? pod Running, DNS/Grafana/Alertmanager/Loki/Vault(unsealed)/Zabbix(web 200) ?????? ? — ??? Zabbix hosts/history + Vault secrets ???? **???????** (???? re-config)

**Lessons Learned:**
- ?? **Proxmox backup ??? master(200)+worker-01(201) ?????????????? 24 ?.?.** ("Broken pipe" — disk ????) ????? .log ????? .vma.zst ? ??????? backup ????
- k3s ??? SQLite (kine) ?????? etcd ? ????? snapshot ??? DR doc ????
- local-path PV data ???????? k3s-uninstall — ?????????? NFS/Longhorn ???? backup
- ???? uninstall/killall ?????????? install type ????
- helm/zabbix/values.yaml corrupt ?? git (??????? 68e2658)
- worker-01 host-port **??? block ???? clean reinstall** ? ????????? VM-level ?????? k3s

## [2026-06-16] — Proxmox hosts reboot, VM ?????????? (cluster ??? ~28h)

**Problem:**
TargetDown firing ???? — ????? ??????? cluster ??? master ping/SSH ??????

**Root Cause:**
pve01 + pve02 reboot ~1d4h ???? (??????????) ??? VM ????????????? `onboot` ? ???? stopped ???

**Fix:**
- start VM ???????? (master/dns ? workers) ? 3 nodes Ready
- unseal Vault, pods ???
- ???? `onboot 1` + startup order ??? VM ??????????

**Result:**
Cluster ?????????? ? ?????????????? VM ???????

**Lessons Learned:**
- ???????? onboot ???????????? VM
- Vault auto-unseal ?????????? (power event = ???? unseal ???????????)
- Alert ????? "cluster down" summary + group/throttle ??? + ??? Windows host ?????? k8s

## [2026-06-08] — Master OOM ?????????? worker-01 + ????? RAM ???? 6GB

**Problem:**
????????? worker-01 host-port (9100/10250 block) ??? business hours ? flush iptables + restart k3s ??? node drained ? pods ????????? master ? master (4GB) OOM ? control-plane ??? (CoreDNS/apiserver TargetDown 100%, alert burst)

**Root Cause:**
- master RAM 4GB ???????? workload ????? worker-01 ?????? scheduling
- worker-01 ?? systemd unit ?????? (k3s.service + leftover k3s-agent.service) ????? kube-router rebuild ???????? — ?????????? worker-01 ??? recover ?????? worker-02

**Fix:**
- `qm reboot 200` + ????? RAM master 4GB?6GB (`qm set 200 --memory 6144`) ? ??? OOM ????
- uncordon worker-01 + restart k3s ? cluster ??????
- unseal Vault + ?? kubescape failed job
- worker-01 host-port ??????? ? ???? reinstall k3s agent ?? maintenance window

**Result:**
Cluster ??????, DNS/apiserver/Vault ?????????? ? — ????? worker-01 metrics (cosmetic, TargetDown ×2)

**Lessons Learned:**
- ???? flush iptables/restart k3s ??? master RAM ??? — reschedule storm ?? OOM control-plane
- master ?????? RAM =6GB
- Standalone Vault ??? auto-unseal — ???? unseal ??????? restart
- worker-01 ????? host-port ???? systemd/k3s unit conflict ???? clean reinstall

## [2026-06-04] — Vault dev-mode ?? config ?????? restart (fastapi-vault 403)

**Problem:**
fastapi-vault ???? Init — vault-agent auth ??????? (403), role/secret ???

**Root Cause:**
Vault deploy ??? `server.dev.enabled: true` (inmem storage) ? restart ??? rolling reboot ????????????? config ??????

**Fix:**
??????????? standalone + file storage (PVC) ???? `helm/vault/values.yaml` + bootstrap ???? `scripts/bootstrap-vault.sh` (init/unseal/enable k8s auth/role/secret) — k8s auth config ??????? `token_reviewer_jwt` + `kubernetes_ca_cert` + `disable_iss_validation` ??????? 403

**Result:**
fastapi-vault 2/2 Running ? — config persist ???? restart ????

**Lessons Learned:**
- ??????? Vault dev mode ???????????????? restart
- Standalone Vault ??? auto-unseal — ???? unseal ??????? restart (??????? auto-unseal)
- ???? bootstrap ???? code ?? repo ???? reproduce ???

## [2026-06-04] — metrics-server scrape worker kubelet ?????? (OPEN)

**Problem:**
`kubectl top nodes/pods` ???? `<unknown>` ???? 2 workers — metrics-server timeout ???? kubelet:10250

**Root Cause:**
SYN ??? worker ??? kernel ?????? SYN-ACK — packet ??? drop ?? `KUBE-ROUTER-INPUT` chain (k3s built-in netpol) ??????? socket ?????? UFW (????? ufw/iptables allow ???????????)

**Fix:**
????????? — deferred ?? maintenance window (???? `systemctl restart k3s` ?? master, ?????? API server ????????????)

**Result:**
Cosmetic ???????? — workload ???????????, node metrics ????????? Prometheus node-exporter ???

**Lessons Learned:**
- kube-router netpol drop ???????? UFW chain — patch ufw ???????
- ???? restart kube-router/k3s ????? rebuild ruleset

## [2026-06-03] — VXLAN blocked by UFW - Zabbix Web Unreachable

**Problem:**
Zabbix web UI (`10.0.1.10:30080`) ????????? — cross-node pod networking ???

**Root Cause:**
UFW INPUT policy = DROP ????? node ? VXLAN (UDP 8472) ??? block + TCP 6443 ??? block ????? workers rejoin cluster ??????

**Fix:**
```bash
# All nodes - VXLAN
sudo ufw allow from 10.0.1.x/24 to any port 8472 proto udp
# Master - API server
sudo ufw allow from 10.0.1.x/24 to any port 6443 proto tcp
```
+ restart k3s-agent on worker-01 (fix stale cni0 bridge IP)

**Result:**
All nodes Ready, Zabbix web HTTP 200 ?

**Lessons Learned:**
- ???? L3/L4 connectivity ???? NetworkPolicy ????
- Document required firewall ports ?????? k3s cluster
