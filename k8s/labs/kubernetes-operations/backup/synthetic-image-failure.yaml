apiVersion: batch/v1
kind: Job
metadata:
  name: backup-image-failure
  namespace: <namespace>
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: backup
          image: postgres:not-a-real-tag
